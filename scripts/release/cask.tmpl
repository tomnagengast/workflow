# This file is generated by the workflow release pipeline. DO NOT EDIT.
cask "workflow-cli" do
  version "{{VERSION}}"

  on_macos do
    on_intel do
      sha256 "{{SHA256_DARWIN_AMD64}}"
      url "https://github.com/{{REPO}}/releases/download/v#{version}/workflow_#{version}_darwin_amd64.tar.gz"
    end
    on_arm do
      sha256 "{{SHA256_DARWIN_ARM64}}"
      url "https://github.com/{{REPO}}/releases/download/v#{version}/workflow_#{version}_darwin_arm64.tar.gz"
    end
  end

  on_linux do
    on_intel do
      sha256 "{{SHA256_LINUX_AMD64}}"
      url "https://github.com/{{REPO}}/releases/download/v#{version}/workflow_#{version}_linux_amd64.tar.gz"
    end
    on_arm do
      sha256 "{{SHA256_LINUX_ARM64}}"
      url "https://github.com/{{REPO}}/releases/download/v#{version}/workflow_#{version}_linux_arm64.tar.gz"
    end
  end

  name "workflow-cli"
  desc "Standalone runner for Claude Code dynamic workflow scripts"
  homepage "https://github.com/{{REPO}}"

  livecheck do
    skip "Auto-generated on release."
  end

  binary "workflow"

  postflight do
    if OS.mac?
      system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/workflow"]
    end
  end
end
